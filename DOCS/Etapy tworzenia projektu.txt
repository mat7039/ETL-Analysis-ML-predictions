1. Napisanie kodu w języku Python, który odczytuje odpowiednie klasy z kodu html z podanego linku, zapisuje podane w ogłoszeniu informacje oraz parametry mieszkania. Zapisanie danych jako DataFrame;

2. Wstępna obróbka pozyskanych danych, usunięcie zbędnych kolumn, modyfikacja sposobu zapisu wartości, zmiana typu danych w kolumnach, zmiana nazw kolumn. Przygotowanie danych do dalszej analizy oraz do sprawnego utworzenia bazy danych w postgreSQL

3. Przygotowanie bazy danych - stworzenie bazy danych, stworzenie tabeli z ilością kolumn, ich nazwą oraz typem danych odpowiadającym danym z modyfikowanego DataFramie w punkcie 2.

4. Napisanie kodu, który odczytuje dane zapisane w pliku csv, oraz łączy się z bazą danych, następnie wgrywa dane z pliku do tabeli SQL.

5. Normalizacja bazy danych oraz eksploracyjna analiza danych za pomocą języka zapytań SQL

6. Wizualizacja najważniejszych zależności wynikających z wcześniejszej analizy - Python

7. Przygotowanie danych do modelowania oraz zastosowanie modeli uczenia maszynowego

8. Stworzenie aplikacji w oparciu o modele uczenia maszynowego